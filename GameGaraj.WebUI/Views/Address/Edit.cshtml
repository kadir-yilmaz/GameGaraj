@model GameGaraj.WebUI.Models.Addresses.UpdateUserAddressInput
@{
    ViewData["Title"] = "Adres Düzenle";
    var isDelivery = Model.Type == GameGaraj.WebUI.Models.Addresses.AddressType.Delivery;
}

<div class="container my-5">
    <div class="row justify-content-center">
        <div class="col-lg-8">
            
            <div class="card shadow-sm">
                <div class="card-header bg-primary text-white">
                    <h5 class="mb-0">
                        <i class="fas fa-edit me-2"></i>
                        @(isDelivery ? "Teslimat Adresi Düzenle" : "Fatura Adresi Düzenle")
                    </h5>
                </div>
                <div class="card-body">

                    <form method="post" asp-action="Edit">
                        <input type="hidden" asp-for="Id" />
                        <input type="hidden" asp-for="Type" />

                        <!-- Kişisel Bilgiler -->
                        <h6 class="text-muted mb-3">Kişisel Bilgiler</h6>
                        
                        <div class="row g-3 mb-4">
                            <div class="col-md-6">
                                <label class="form-label">Ad <span class="text-danger">*</span></label>
                                <input class="form-control" asp-for="FirstName" required />
                                <span asp-validation-for="FirstName" class="text-danger small"></span>
                            </div>
                            <div class="col-md-6">
                                <label class="form-label">Soyad <span class="text-danger">*</span></label>
                                <input class="form-control" asp-for="LastName" required />
                                <span asp-validation-for="LastName" class="text-danger small"></span>
                            </div>
                            <div class="col-md-6">
                                <label class="form-label">Telefon <span class="text-danger">*</span></label>
                                <input class="form-control" asp-for="PhoneNumber" placeholder="+90 555 123 4567" required />
                                <span asp-validation-for="PhoneNumber" class="text-danger small"></span>
                            </div>
                            <div class="col-md-6">
                                <label class="form-label">E-posta <span class="text-danger">*</span></label>
                                <input type="email" class="form-control" asp-for="Email" placeholder="ornek@email.com" required />
                                <span asp-validation-for="Email" class="text-danger small"></span>
                            </div>
                        </div>

                        <hr class="my-4" />

                        <!-- Adres Bilgileri -->
                        <h6 class="text-muted mb-3">Adres Bilgileri</h6>

                        <div class="row g-3 mb-4">
                            <div class="col-md-6">
                                <label class="form-label">İl <span class="text-danger">*</span></label>
                                <input class="form-control" asp-for="Province" placeholder="Örn: İstanbul" required />
                                <span asp-validation-for="Province" class="text-danger small"></span>
                            </div>

                            <div class="col-md-6">
                                <label class="form-label">İlçe <span class="text-danger">*</span></label>
                                <input class="form-control" asp-for="District" placeholder="Örn: Kadıköy" required />
                                <span asp-validation-for="District" class="text-danger small"></span>
                            </div>

                            <div class="col-md-6">
                                <label class="form-label">Mahalle <span class="text-danger">*</span></label>
                                <input class="form-control" asp-for="Neighborhood" placeholder="Mahalle adı" required />
                                <span asp-validation-for="Neighborhood" class="text-danger small"></span>
                            </div>

                            <div class="col-md-6">
                                <label class="form-label">Posta Kodu</label>
                                <input class="form-control" asp-for="PostalCode" maxlength="5" placeholder="34000" />
                                <span asp-validation-for="PostalCode" class="text-danger small"></span>
                            </div>

                            <div class="col-12">
                                <label class="form-label">Adres Detayı <span class="text-danger">*</span></label>
                                <textarea class="form-control" asp-for="AddressDetail" rows="3" 
                                          placeholder="Sokak, bina no, daire no..." required></textarea>
                                <span asp-validation-for="AddressDetail" class="text-danger small"></span>
                            </div>

                            <div class="col-md-6">
                                <label class="form-label">Adres Başlığı <span class="text-danger">*</span></label>
                                <input class="form-control" asp-for="Title" placeholder="Örn: Evim, İşyerim" required />
                                <span asp-validation-for="Title" class="text-danger small"></span>
                            </div>

                            <div class="col-md-6">
                                <div class="form-check mt-4">
                                    <input class="form-check-input" type="checkbox" asp-for="IsDefault" />
                                    <label class="form-check-label" asp-for="IsDefault">
                                        Varsayılan adres olarak ayarla
                                    </label>
                                </div>
                            </div>
                        </div>

                        <div class="d-flex gap-2">
                            <a asp-action="Index" asp-route-tab="@Model.Type" class="btn btn-outline-secondary">
                                <i class="fas fa-times me-1"></i> Vazgeç
                            </a>
                            <button type="submit" class="btn btn-primary">
                                <i class="fas fa-check me-1"></i> Güncelle
                            </button>
                        </div>

                    </form>
                </div>
            </div>
            
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
